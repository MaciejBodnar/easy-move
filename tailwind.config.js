export default {
  theme: {
    fontFamily: {
      // Primary sans-serif for body and most content
      sans: ['system-ui', '-apple-system', 'Segoe UI', 'Roboto', 'sans-serif'],
      // TypeKit fonts will override via CSS if the kit provides them
      serif: ['Georgia', 'serif'],
      // For system monospace text
      mono: ['ui-monospace', 'monospace'],
    },
  },
};
